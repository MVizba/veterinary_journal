{% extends 'base.html.twig' %}

{% block body %}
    <h1>Add Examination</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">Save Examination</button>
    {{ form_end(form) }}
{% endblock %}
